export { sanityClient, sanityWriteClient, urlForImage, config } from "./client";
export { schemaTypes, product, navigation } from "./schemas";

// Re-export types
export type { SanityImageSource } from "@sanity/image-url/lib/types/types";
